@model IEnumerable<WebAdminConsole.ViewModels.StageResultsViewModel>

@if (ViewData["StageName"] != null)
{
    ViewData["Title"] = ViewData["StageName"];
    <h2 class="control-label">@ViewData["StageName"]</h2>
}

@if (ViewData["NoResults"] != null)
{
    ViewData["Title"] = ViewData["NoResults"];
    <h3 class="text-danger">@ViewData["NoResults"]</h3>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Position)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Time)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Difference)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.BibNumber.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Runner.First)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Runner.Last)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Runner.Teams.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Runner.Category.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CatPosition)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.CatDifference)
                </th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Position)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Time)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Difference)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.BibNumber.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Runner.First)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Runner.Last)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Runner.Teams.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Runner.Category.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CatPosition)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.CatDifference)
                    </td>
                </tr>
            }
        </tbody>
    </table>
}